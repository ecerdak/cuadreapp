{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @cuadreapp/admin build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @cuadreapp/admin start",
    "healthcheckPath": "/salud",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
